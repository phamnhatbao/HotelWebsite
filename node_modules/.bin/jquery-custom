#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jquery-custom/bin/jquery-custom" "$@"
  ret=$?
else 
  node  "$basedir/../jquery-custom/bin/jquery-custom" "$@"
  ret=$?
fi
exit $ret
